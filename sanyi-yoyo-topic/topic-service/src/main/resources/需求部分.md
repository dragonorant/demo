```text
 首页 -- 应该是从后端获取的 联网模块列表 --  此处交由 app 端进行控制, 
    显示部分需要由 app 端支持, 也就是由 app 端做显示所有的 后端联网模块入口
             
```

## 主题赛
```text
 主题赛 -- 同时可以查看多个，但是同时只会存在一个, 即 可以根据时间查看 往期的 主题赛 以及 将来的主题赛
 
            主题列表 分为活动时间、评分领奖时间。 在主题首页直接可以上传作品。
            
            主题上传 -- 具体上传数据由 app 端进行控制，后端只负责存储
            
            上传作品 -- 每天可以上传一次，刷新时间为 utc+0 的0点
                       首先请求后台是否拥有上传资格，true 可以上传，调用下一个接口
                       
            主题数据列表 -- 分为 最新、 最热、查看我自己(时间倒叙) -- (本期上传)
            
            获奖 -- 记录 -- 内部邮件 -- 发送奖励 （文本 + 附件）
```